命令 cd 的基本用法：

1.在进入 git-bush之后，默认为在git文件夹下
2.直接使用 cd 会退到 C/Users/levon 的地方
3.在 cd 后空格 并使用 Tab 指令
  可以弹出（y or n） 的选项 y 来显示出该文件目录下的所有的文件
4.在 cd 后 打出首字母并 Tab 会自动补全，有多个选项的 Tab会显示这些选项但不会补全了
  在以上（目标锁定）的基础上再按 Tab会显示该目录下的文件
5.cd .. 是退出到上一层
6.cd xxx/xxx/xxx 路径不解释了
